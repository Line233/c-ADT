#include<stdio.h>
#include"status.h"


int main(void)
{
    // //read data from in.txt to a list
    // FILE *r;
    // r=fopen("input.txt","r");
    // if(r==NULL)
    //     {
    //         printf("error:open failed");
    //         return ERROR;
    //     }
    // int n;
    // feof(r);
    // fscanf(r,"%d",&n);

    // getchar();
}